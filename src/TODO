TODO:

file systems:
* FAT
* NTFS

compression:
* rzip
* E00

string extraction:
* .txx (templates, see insight toolkit for examples)

queueing system:
* let top level script also read from a queue
* add script to add files to and remove files from scanning queue

packing:
* introduce new configuration parameters that control which result files should be packed and where they should be written to
