TODO:

file systems:
* FAT
* NTFS

compression:
* rzip
* E00

string + function name extraction:
* .txx (templates, see insight toolkit for examples)
* rewrite ctags invocation to use -x and also extract line numbers of function names

queueing system:
* let top level script also read from a queue
* add script to add files to and remove files from scanning queue

postrunscans:
* support "noscan"
* rework code to avoid duplicate processing
* aggregate ranking results for JAR file scanning
